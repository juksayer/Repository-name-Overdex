package com.example.overdex.model.observation

/**
 * Enumeration of stable UI elements that can serve as visual anchors.
 */
enum class AnchorType {
    MoveIcon,
    FastMoveIcon,
    ChargedMoveIcon,
    TypeBadge,
    Other
}
