package com.example.overdex.battle.timeline.observer

/**
 * Defines the physical or digital origin of an observation.
 */
enum class ObservationSource {
    SCREEN_CAPTURE,
    AUDIO_CAPTURE,
    DROIDBALL,
    REMOTE_PARTNER,
    SYSTEM_INFERENCE,
    TRAINER,
    ORGANIZER,
    SYSTEM
}
